# Regras do Projeto

Sempre que concluir alguma tarefa ou alteração solicitada no projeto, faça o `git add`, `git commit` e `git push` automaticamente para garantir que o repositório remoto esteja atualizado, sem esperar que o usuário peça.
